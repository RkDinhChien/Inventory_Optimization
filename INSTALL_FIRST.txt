#!/bin/bash

cat << 'EOF'

╔════════════════════════════════════════════════════════════════════════════╗
║                    🚨 HƯỚNG DẪN SETUP - BẮT BUỘC                          ║
╚════════════════════════════════════════════════════════════════════════════╝

Hiện tại bạn KHÔNG THỂ chạy Python vì thiếu Xcode Command Line Tools.

┌────────────────────────────────────────────────────────────────────────────┐
│  BƯỚC 1: CÀI ĐẶT XCODE COMMAND LINE TOOLS (BẮT BUỘC)                      │
└────────────────────────────────────────────────────────────────────────────┘

1️⃣  Mở Terminal và chạy lệnh này:

    xcode-select --install

2️⃣  Một cửa sổ popup sẽ xuất hiện:
    ┌─────────────────────────────────────┐
    │  Install Command Line Developer     │
    │  Tools?                             │
    │                                     │
    │  [Not Now]  [Install]              │
    └─────────────────────────────────────┘

3️⃣  Click nút [Install]

4️⃣  Đợi 5-10 phút để tải và cài đặt

5️⃣  Click [Done] khi hoàn tất

┌────────────────────────────────────────────────────────────────────────────┐
│  BƯỚC 2: XÁC NHẬN CÀI ĐẶT THÀNH CÔNG                                      │
└────────────────────────────────────────────────────────────────────────────┘

Chạy lệnh này để kiểm tra:

    xcode-select -p

Nếu thấy output như này → THÀNH CÔNG:
    /Library/Developer/CommandLineTools

┌────────────────────────────────────────────────────────────────────────────┐
│  BƯỚC 3: CÀI ĐẶT PYTHON DEPENDENCIES                                      │
└────────────────────────────────────────────────────────────────────────────┘

Sau khi cài xong Xcode Command Line Tools, chạy:

    cd "/Users/rykan/ĐỒ ÁN/Inventory_Optimization"
    pip3 install -r requirements.txt

Hoặc cài từng package:

    pip3 install pandas numpy matplotlib seaborn scipy plotly

Để dùng ML (optional):

    pip3 install scikit-learn statsmodels xgboost prophet

┌────────────────────────────────────────────────────────────────────────────┐
│  BƯỚC 4: CHẠY DEMO                                                         │
└────────────────────────────────────────────────────────────────────────────┘

Sau khi cài xong dependencies:

    # Demo nhanh (không cần ML)
    python3 demo_quick.py

    # So sánh các ML algorithms
    python3 demo_ml.py

    # Test hệ thống
    python3 test_simple.py

    # Chạy hệ thống chính
    python3 main.py

┌────────────────────────────────────────────────────────────────────────────┐
│  ⚠️  TẠI SAO CẦN XCODE COMMAND LINE TOOLS?                                │
└────────────────────────────────────────────────────────────────────────────┘

macOS cần tools này để:
  • Compile Python native extensions (numpy, pandas, etc.)
  • Build C/C++ libraries
  • Link system libraries
  
KHÔNG CÓ NÓ → Python không chạy được!

┌────────────────────────────────────────────────────────────────────────────┐
│  💡 GẶP VẤN ĐỀ?                                                            │
└────────────────────────────────────────────────────────────────────────────┘

Lỗi: "xcode-select: command not found"
→ Bạn đang dùng macOS, lệnh này luôn có sẵn. Thử lại.

Lỗi: "Can't install the software because it is not currently available"
→ Check internet connection hoặc thử:
   sudo rm -rf /Library/Developer/CommandLineTools
   xcode-select --install

Lỗi: "pip3: command not found" 
→ Sau khi cài Xcode, chạy:
   python3 -m ensurepip --upgrade

┌────────────────────────────────────────────────────────────────────────────┐
│  📚 TÀI LIỆU THAM KHẢO                                                     │
└────────────────────────────────────────────────────────────────────────────┘

Sau khi setup xong, đọc:
  • QUICKREF.txt    - Quick reference commands
  • SETUP_MACOS.md  - Chi tiết setup macOS
  • ML_GUIDE.md     - Hướng dẫn ML algorithms
  • SUMMARY.md      - Tổng quan dự án

╔════════════════════════════════════════════════════════════════════════════╗
║  🎯 TÓM LẠI: CÀI XCODE COMMAND LINE TOOLS TRƯỚC, SAU ĐÓ MỌI THỨ SẼ CHẠY  ║
╚════════════════════════════════════════════════════════════════════════════╝

EOF
